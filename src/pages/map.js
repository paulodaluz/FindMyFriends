import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Map = (props) => {
  return (
    <View>
      <Text> MAPA DE AMIGOS </Text>
    </View>
  );
};

export default Map;

const styles = StyleSheet.create({});
